# 모의 면접 데모 영상 촬영용 대본 (script.md)

이 문서는 촬영 스태프와 연기자(면접관, 멘티들)가 보고 면접을 연기하기 위한 가독성 높은 촬영용 대본입니다.

---

## 1. 채용공고 (Job Postings)

### [공고 1] 대규모 트래픽 분산 처리 백엔드 개발자 (최지아 지원 분야)
* **업무 자격요건**
  * Java 및 Spring Boot 기반 백엔드 애플리케이션 개발 역량
  * JPA, Hibernate 등 ORM 프레임워크 활용 및 엔티티 설계 경험
  * RESTful API 설계 규칙에 대한 이해 및 설계 유경험자
  * RDBMS(MySQL 등)의 인덱스 최적화 및 동시성 제어 능력
* **인재상**
  * 장애 발생 시 집요하게 분석하여 해결책을 도출하는 끈기 있는 인재
  * 고품질 아키텍처와 성능 최적화에 대한 책임감을 지닌 신뢰형 인재
  * 끊임없이 새로운 분산 처리 기법을 탐구하고 지식을 공유하는 학습형 인재
* **우대사항**
  * Redis 등 인메모리 DB를 활용한 분산 락 및 캐싱 구축 경험
  * Kafka, RabbitMQ 등 메시지 큐 기반 비동기 파이프라인 연동 경험
  * nGrinder, JMeter 등을 사용한 부하 테스트 및 모니터링 메트릭 분석 경험
  * 무중단 배포(Blue-Green, Canary) 아키텍처 설계 경험자

### [공고 2] AI 파이프라인 서빙 백엔드 개발자 (정윤진 지원 분야)
* **업무 자격요건**
  * Python 및 FastAPI / Flask 기반의 백엔드 서비스 개발 역량
  * RDBMS(PostgreSQL 등)의 기본적인 설계 및 쿼리 튜닝 능력
  * 비동기 태스크 큐 프레임워크(Celery, asyncio)의 작동 방식 이해
  * 대용량 미디어 파일(오디오, 비디오) 처리 및 데이터 전처리 지식
* **인재상**
  * 타 부서(AI 모델러, 기획자 등)와의 원활한 소통을 이끄는 협업형 인재
  * 신규 프레임워크 도입 전 안정성을 철저히 기하는 신중형 인재
  * 복잡한 파이프라인의 구조적 문제를 발견하고 혁신을 시도하는 개척자형 인재
* **우대사항**
  * RabbitMQ, Redis Pub/Sub 등 메시지 브로커를 활용한 대기열 구축 경험
  * Docker, Alembic 등을 활용한 컨테이너 배포 및 마이그레이션 경험
  * Triton Inference Server, ONNX Runtime 등 모델 서빙 가속화 유경험자
  * 대용량 배치 처리 및 비동기 모델 병렬 처리 아키텍처 설계 경험자

### [공고 3] 초실시간 양방향 통신 백엔드 개발자 (강동현 지원 분야)
* **업무 자격요건**
  * Node.js 및 NestJS 기반 백엔드 애플리케이션 개발 역량
  * WebSocket, Socket.io 등을 활용한 양방향 실시간 웹 통신 구현 경험
  * RDBMS(PostgreSQL 등) 설계 및 데이터 트랜잭션 관리 기본 지식
  * 네트워크 프로토콜(TCP/IP, HTTP) 및 패킷 전송 원리에 대한 이해
* **인재상**
  * 실시간 동기화 오류 및 딜레이 상황에 신속히 대처하는 기민형 인재
  * 네트워크 패킷 및 오버헤드 최적화의 디테일을 고집하는 정밀형 인재
  * 프론트엔드 실시간 클라이언트와의 요구사항을 긴밀하게 조율하는 소통형 인재
* **우대사항**
  * WebRTC(Mediasoup, Kurento) 기반 화상 미디어 스트리밍 구축 경험
  * 다중 Node.js 인스턴스 분산을 위한 Redis Pub/Sub 또는 Socket.io 클러스터링 구축 경험
  * Jest 등을 활용한 실시간 비동기 소켓 이벤트 테스트 코드 작성 경험
  * 소켓 단선 상황에서의 재접속 핸드셰이크 및 세션 복구 아키텍처 설계 경험자

---

## 2. 자기소개서 (Cover Letters)

### 👤 [최지아 지원자 자기소개서]

* **지원동기** (548자)
  저는 대규모 트래픽 환경에서 한 치의 오차도 없이 안정적으로 동작하는 백엔드 시스템을 설계하고 구축하는 것에 깊은 열정을 품고 있습니다. 대학 시절 진행했던 이커머스 플랫폼 토이 프로젝트에서, 대형 할인 이벤트 상황을 가정하고 동시 접속자 수를 증가시키는 부하 테스트를 진행한 적이 있습니다. 이때 서버가 응답 불능 상태에 빠지는 것을 목격하고, 트래픽 폭증이 서비스에 미치는 치명적인 영향력을 뼈저리게 느꼈습니다. 단순한 기능 구현을 넘어, 분산 환경에서의 데이터 정합성 유지와 서버 자원의 최적화가 백엔드 개발자로서의 진정한 전문성이라는 확신을 얻게 되었습니다. 귀사는 혁신적인 모의 면접 서비스를 통해 매일 수만 명의 사용자에게 실시간 영상 및 오디오 스트리밍과 데이터 분석 리포트를 제공하며 급격히 성장하고 있습니다. 이러한 대규모 트래픽 속에서도 무중단 서비스를 제공하고, 아키텍처 수준에서의 성능 튜닝을 통해 인프라 비용을 절감하는 도전에 직접 동참하고자 지원하였습니다. 그동안 학습한 고성능 아키텍처 설계를 바탕으로 서비스의 안정적인 성장에 든든한 초석이 되겠습니다.

* **본인의 강점과 약점** (507자)
  저의 가장 큰 강점은 복잡한 성능 장애 상황에서 원인을 집요하게 파헤치는 분석력과 끈기입니다. 문제 상황을 마주했을 때 감에 의존하지 않고, Prometheus와 Grafana 같은 모의 모니터링 도구를 활용해 스레드 덤프와 힙 메모리 덤프를 분석하며 근본적인 병목 지점을 수치화해 찾아내는 것을 즐깁니다. 이를 통해 동시성 문제를 명확히 제어하며 시스템을 견고하게 개선해 나갑니다. 반면, 저의 약점은 아키텍처의 완벽성과 성능 최적화에 지나치게 몰두한 나머지 개발 마일스톤에 명시된 초기 일정 설정을 다소 타이트하게 가져가는 경향이 있다는 점입니다. 과도한 최적화로 인해 마케팅이나 기획 부서와의 일정 협의 과정에서 마찰이 생기지 않도록, 최근에는 MVP 개발 단계와 성능 고도화 단계를 철저히 분리하여 기록하는 습관을 들이고 있습니다. 이를 통해 비즈니스 요구사항과 기술적 고도화 사이의 균형 감각을 기르며, 협업 부서의 일정 조율 요구사항에 보다 유연하게 대처할 수 있도록 스스로를 보완하고 있습니다.

* **실무/프로젝트 문제 해결 경험** (562자)
  이커머스 웹 애플리케이션 프로젝트에서 한정판 선착순 쿠폰 발급 이벤트를 진행할 때 발생했던 심각한 동시성 이슈와 데이터 정합성 문제를 해결한 경험이 있습니다. 초기에 아무런 락 장치 없이 이벤트를 오픈하자 데이터베이스에 수많은 쓰기 요청이 몰려 커넥션 풀이 순식간에 고갈되었고, 최종적으로 준비된 쿠폰 수량보다 더 많은 쿠폰이 발급되는 동시성 정합성 위반 오류가 발생했습니다. 문제를 해결하기 위해 먼저 Redis의 분산 락(Redisson)을 도입하여 동일한 사용자와 한정된 자원에 대해 원자적 연산을 보장하도록 개선했습니다. 또한, DB에 가해지는 직접적인 부하를 낮추기 위해 Lua 스크립트를 작성하여 남은 쿠폰 수량의 차감을 메모리 상의 Redis 내에서 1차로 검증하도록 변경하였고, 통과된 요청에 한해서만 Kafka 메시지 큐를 통해 DB에 비동기적으로 기록하도록 파이프라인을 설계했습니다. 그 결과, 초당 3,000건의 동시 요청이 발생하는 상황에서도 쿠폰의 초과 발급 없이 데이터 정합성을 완벽히 유지했으며, 평균 API 응답 시간 또한 기존 대비 약 75% 단축시키는 성과를 거두었습니다.

---

### 👤 [정윤진 지원자 자기소개서]

* **지원동기** (507자)
  저는 대용량 원천 데이터로부터 유의미한 가치를 도출하고 이를 서비스화하는 AI 데이터 파이프라인 구축 분야에 큰 흥미를 느끼고 있습니다. 학부 연구생 시절, 공공 영상 데이터를 기반으로 객체를 탐지하고 분류하는 모델을 백엔드 서버에 서빙하는 프로젝트를 진행한 적이 있습니다. 당시 단일 요청 처리에는 문제가 없었으나, 수백 명의 사용자가 동시에 동영상을 업로드하자 모델의 긴 추론 속도로 인해 웹 서버 전체가 동결되는 장애를 경험했습니다. 이 일을 계기로 AI 모델의 우수성만큼이나 이를 안정적이고 효율적인 비동기 파이프라인으로 서빙하는 백엔드 아키텍처가 필수적임을 깨달았습니다. 귀사는 인공지능 기반의 면접 분석 피드백을 통해 멘티들의 역량을 정량화하여 제시하는 시장 선두의 기술력을 확보하고 있습니다. 모델의 응답 속도 지연을 줄이고, 다양한 원천 데이터를 실시간으로 수집하고 정제하여 대규모 AI 분석 연산을 안정적으로 배포하는 파이프라인 아키텍처를 직접 설계하고 최적화하고 싶어 지원하게 되었습니다.

* **본인의 강점과 약점** (511자)
  저의 큰 강점은 기획자나 데이터 사이언티스트와의 적극적인 소통을 통해 기술 요구사항을 명확히 명세화하고 조율하는 협업 능력입니다. AI 서빙 파이프라인을 다룰 때는 백엔드 개발자 혼자만의 생각보다 모델을 만든 사이언티스트와의 데이터 포맷 싱크와 리소스 한계 설정이 매우 중요하기 때문에, 적극적인 피드백을 통해 중간 조율자 역할을 성실히 수행합니다. 반면, 약점은 새로운 AI 기술 트렌드와 분산 프레임워크를 빠르게 도입하려는 의욕이 앞선 나머지, 기존 아키텍처와의 통합 테스트나 호환성 검증 프로세스를 가끔 성급하게 진행하는 면이 있다는 점입니다. 이를 보완하기 위해 신규 기술이나 라이브러리를 실제 프로젝트에 도입하기 전에, 반드시 별도의 샌드박스 개발 격리 환경을 구축하여 호환성 및 부하 테스트를 선행하도록 규칙을 정했습니다. 테스트 결과를 상세히 정리한 위키 문서를 팀원들과 공유함으로써, 성급한 도입으로 인한 부작용을 사전에 철저히 차단하고 시스템의 안정성을 해치지 않도록 지속적으로 관리하고 있습니다.

* **실무/프로젝트 문제 해결 경험** (580자)
  AI 기반의 면접 평가 서비스 프로젝트에서 멘티의 음성 데이터(WAV 파일)를 분석하여 감정 및 텍스트를 추출하는 대용량 AI 분석 파이프라인을 비동기로 최적화한 경험이 있습니다. 초기에 오디오 파일이 업로드되면 백엔드 서버가 동기적으로 AI 모델 API를 호출하여 결과를 대기하는 방식으로 구현했으나, 파일의 크기가 클 경우 백엔드 서버에 타임아웃이 나고 오디오 스트림 처리가 끊기는 병목 현상이 발생했습니다. 이 문제를 해결하기 위해 Celery와 RabbitMQ 기반의 비동기 메시지 큐 아키텍처를 전격 도입했습니다. 사용자가 음성을 업로드하면 백엔드는 즉시 작업 ID를 반환하고, 오디오 원천 파일은 S3에 저장한 후 Celery 워커가 메시지를 수신하여 비동기적으로 Whisper와 감정 분석 모델을 병렬 처리하도록 구조를 이원화했습니다. 또한, 분석 진행 상황을 클라이언트가 실시간으로 폴링할 수 있도록 Redis에 작업 상태를 캐싱하는 데이터 레이어를 추가했습니다. 이를 통해 백엔드 서버의 커넥션 점유 시간을 제로로 만들었고, 병렬 분석 처리를 도입하여 분석 완료 대기 시간을 이전 동기식 대비 약 60% 이상 줄였습니다.

---

### 👤 [강동현 지원자 자기소개서]

* **지원동기** (501자)
  저는 다수의 사용자가 동시에 참여하여 실시간으로 소통할 수 있는 양방향 미디어 및 데이터 통신 아키텍처 구현에 큰 매력을 느낍니다. 학과 캡스톤 디자인 프로젝트로 실시간 다인용 화이트보드 협업 툴을 개발하면서, 여러 사용자가 동시에 캔버스를 그릴 때 발생하는 메시지 순서 꼬임과 실시간 동기화 지연 현상을 목격했습니다. 클라이언트 간의 네트워크 레이턴시를 최소화하고, 서버의 리소스를 효율적으로 분배하여 끊김 없는 상호작용을 보장하는 기술이 웹 서비스의 사용자 경험을 극대화한다는 것을 확인했습니다. 귀사는 모의 면접 화상 통신과 실시간 면접 평가 분석을 결합하여, 공간의 제약 없이 고품질의 실시간 면접 시뮬레이션을 가능하게 하는 서비스를 선도적으로 개발하고 있습니다. 제가 실시간 통신 환경에서 쌓아온 소켓 분산 처리 및 미디어 서버에 대한 지식을 귀사의 서비스에 접목하여, 대규모 그룹 면접에서도 끊김이나 딜레이 없이 완벽한 몰입감을 선사하는 초실시간 양방향 면접 환경을 구현하고 싶습니다.

* **본인의 강점과 약점** (543자)
  저의 큰 기술적 강점은 네트워크 프로토콜과 패킷 흐름에 대한 깊은 이해를 바탕으로 실시간 병목 구간을 빠르게 진단해 내는 능력입니다. WebSocket 통신에서 발생하는 프레임 유실이나 패킷 지연을 Wireshark나 Chrome DevTools로 실시간 추적하고, 효율적인 바이너리 직렬화 포맷을 도입해 전송 오버헤드를 낮추는 등의 네트워크 최적화 작업을 즐깁니다. 반면, 저의 약점은 실시간 통신 기능의 즉각적인 구현에 집중하다 보니 데이터의 장기적인 영속성 보장이나 관계형 데이터베이스(RDB)의 엄격한 스키마 설계 및 데이터 관리 지식에서 상대적인 깊이가 조금 부족하다는 점입니다. 이러한 단점을 보완하기 위해 프로젝트 진행 시 RDB의 트랜잭션 격리 수준(Isolation Level)과 관계형 데이터 모델링 규칙을 의도적으로 학습하고 있습니다. 최근에는 채팅 이력 데이터를 PostgreSQL에 안전하게 저장하기 위해 Write-Ahead Logging(WAL) 구조와 인덱스 스캔 방식을 깊이 스터디하여 데이터 정합성 보장 역량을 다각도로 넓혀가고 있습니다.

* **실무/프로젝트 문제 해결 경험** (560자)
  NestJS와 Socket.io를 사용하여 다수의 사용자가 동시에 참여하는 실시간 다인용 화상 모의 면접 룸의 이벤트 동기화 서버를 구축하고 성능을 개선한 프로젝트 경험이 있습니다. 초기에는 단일 인스턴스 서버로 구축했으나, 면접 룸의 개수와 참여자 수가 일정 수준을 넘어서자 CPU 점유율이 90% 이상으로 치솟으며 클라이언트 소켓 연결이 빈번히 유실되고 메시지 전송이 지연되는 부하 현상이 일어났습니다. 저는 이 문제를 다중 서버 분산 아키텍처로 확장하여 해결했습니다. Nginx 로드밸런서를 전면에 배치하여 클라이언트 세션을 분배하고, 다중 NestJS 인스턴스 간의 소켓 메시지 동기화를 위해 Redis Pub/Sub을 브릿지로 연동했습니다. 특히 실시간 데이터 전송 오버헤드를 최소화하기 위해 JSON 형태의 무거운 메시지를 필요한 핵심 정보만 포함하는 커스텀 패킷 구조로 압축했습니다. 그 결과, 단일 서버의 메모리 부하를 약 40% 분산시켰으며, 동시 접속 사용자 수가 기존 한계치 대비 3.5배 증가한 환경에서도 메시지 레이턴시를 80ms 이내로 균일하게 안정화하는 성과를 도출했습니다.

---

## 3. 영상 촬영용 면접 시나리오

### 3.1 공통 질문 진행 (Common Questions)

#### ❓ [공통 질문 1 - 추천 공통 질문 1번 기반]
> **면접관**: "반갑습니다 지원자 여러분. 먼저 공통 질문부터 시작하겠습니다. 각자 프로젝트에서 주력 백엔드 기술(Spring Boot, FastAPI, NestJS)과 인프라 컴포넌트(Redis, RabbitMQ 등)를 선택할 때, 다른 대안 대신 해당 기술을 사용하기로 결정한 핵심적인 아키텍처적 이유는 무엇인가요?"

* **최지아 답변** `[평가 점수: 4.8/5]`
  "대규모 트래픽 처리를 위해 Spring Boot의 멀티스레드 기반 MVC 구조와 안정적인 JPA/트랜잭션 관리 능력을 주력 기술로 선택했습니다. 동시 접속 요청이 몰리는 대형 이벤트 상황을 가정했을 때, 비즈니스 로직의 복잡성과 데이터 정합성 보장이 필수적이었기 때문에 Node.js 등 싱글스레드 진영 대신 JVM 환경이 적합하다고 보았습니다. 또한, 디스크 쓰기 성능의 한계를 극복하기 위해 메모리 상에서 빠른 읽기/쓰기가 가능한 Redis를 인프라 캐싱 컴포넌트로 활용하여 DB의 부하를 원천 차단하는 설계를 채택했습니다."

* **정윤진 답변** `[평가 점수: 3.5/5]`
  "AI 파이프라인 처리를 위해서 파이썬 환경의 AI 모델들과 라이브러리 연동성이 우수한 FastAPI를 주력 기술로 선택했습니다. 대안으로 Django나 Flask도 있지만 비동기 웹소켓과 코루틴 지원이 뛰어난 FastAPI가 모델 추론 대기 환경에 적합하다고 보았습니다. 또한 AI 연산 특성상 단일 서버에서 동기적으로 처리하면 다른 일반 API 응답까지 끊기게 되기 때문에 비동기 메시지 브로커로 Celery와 RabbitMQ를 결합해 무거운 연산을 비동기 태스크 큐로 분리하는 설계를 구축했습니다."

* **강동현 답변** `[평가 점수: 2.0/5]`
  "실시간 소켓 통신을 효율적으로 다루기 위해서 Node.js 환경의 NestJS 프레임워크와 Socket.io 라이브러리를 사용했습니다. 스프링 부트 소켓도 알아봤지만 자바스크립트 기반이 프론트와의 연계도 잘 맞고 구현이 직관적이라고 보았습니다. 그리고 여러 서버를 띄울 경우 소켓 통신 메시지가 서로 교환이 안 될 수 있는 문제가 있어서, 인스턴스 간 브릿지로 Redis의 Pub/Sub을 연결하여 소켓 서버 간 메시지를 동기화하는 구조를 잡았습니다."

---

#### ❓ [공통 질문 2 - 추천 리스트에 없는 추가 공통 질문]
> **면접관**: "네, 잘 들었습니다. 이어서 다음 질문 드리겠습니다. 프로덕션 서버를 실제로 운영한다고 가정했을 때, 실시간으로 발생하는 시스템 장애나 성능 병목을 감지하고 신속하게 원인을 파악하기 위해 어떤 모니터링 메트릭 수집 및 알림 체계를 구축할 것인지 각자의 직무 세부 분야 관점에서 말씀해 주세요."

* **최지아 답변** `[평가 점수: 4.8/5]`
  "프로덕션 대규모 트래픽 환경에서는 Prometheus를 활용해 JVM 메트릭, 즉 액티브 스레드 수, GC 발생 빈도, 힙 메모리 사용량을 실시간으로 수집하고, Grafana 대시보드로 시각화하는 모니터링 체계를 구축하겠습니다. 특히 커넥션 풀 고갈을 예방하기 위해 HikariCP의 활성 커넥션 수와 대기 시간 메트릭을 유심히 관찰할 것입니다. 수집된 메트릭은 Alertmanager와 연동하여 커넥션 획득 대기 시간이 500ms를 초과하거나 API 에러율이 1%를 초과하는 즉시 Slack을 통해 알림이 발송되도록 구성하여 즉각적인 분석 및 대응이 가능한 운영 체계를 마련하겠습니다. 추가로 Pinpoint APM을 도입하여 서비스 간 분산 트레이싱을 연계 분석하겠습니다."

* **정윤진 답변** `[평가 점수: 3.2/5]`
  "AI 파이프라인에서는 CPU와 GPU의 하드웨어 리소스 사용량 및 Celery 워커의 동작 메트릭이 중요합니다. 저는 Celery 대시보드 도구인 Flower를 띄워 실시간 작업 실패율과 태스크 소요 시간을 확인하겠습니다. 또한 RabbitMQ 매니지먼트 플러그인을 활용하여 큐에 쌓인 메시지 수(Queue Depth)가 너무 늘어나는 병목 상황을 모니터링하겠습니다. 만약 특정 임계치를 넘는 경우에 알림을 연동하는 방식을 채택하고자 합니다."

* **강동현 답변** `[평가 점수: 1.8/5]`
  "서버가 중단되거나 지연되는 현상을 막는 게 중요하다고 생각해서, 서버 내부 코드에 console.log 등으로 로그를 남기려 했습니다. 그리고 실무용 모니터링 도구는 따로 써보진 못했고 대신 서버가 불의의 에러로 갑자기 죽으면 즉시 자동으로 재시작을 시켜주는 PM2 같은 프로세스 매니저를 서버에 띄워서 장애에 임시로 대응하려 해보았습니다."

---

### 3.2 개인 질문 진행 (Personal Questions)

#### 👤 멘티 1: 최지아 (대규모 트래픽 분야)

#### ❓ [개인 질문 1 - 추천 개인 질문 1번 기반]
> **면접관**: "최지아 지원자님께 질문 드리겠습니다. 한정판 선착순 쿠폰 발급 프로젝트에서 Redis의 Redisson 분산 락을 선택하셨는데, Lettuce의 스핀 락 방식 대신 Redisson의 Pub/Sub 기반 락을 선택하여 얻은 성능적 이점은 구체적으로 무엇인가요?"

* **최지아 답변** `[평가 점수: 4.9/5]` *(최우수 답변 - BEST 문항)*
  "Lettuce의 스핀 락 방식은 락을 획득할 때까지 지속적으로 Redis에 `SETNX` 요청을 폴링하기 때문에 락 획득 시도를 할 때마다 Redis의 CPU 리소스를 낭비하고 네트워크 오버헤드를 발생시킵니다. 반면, Redisson은 Redis의 Pub/Sub 기능을 활용합니다. 락을 획득하지 못한 클라이언트는 락 해제 채널을 구독하고 대기하다가, 기존 락 보유자가 락을 해제하며 발행하는 메시지를 수신했을 때 한 번만 다시 획득 시도를 하므로 불필요한 폴링 부하를 완벽히 해소합니다. 또한, 내부적으로 보관 연장 메커니즘을 제공하여 비즈니스 로직 처리 중 락 만료로 인한 동시성 깨짐을 안전하게 예방할 수 있는 강점이 있습니다."

#### ❓ [개인 질문 2 - 추천 리스트에 없는 추가 개인 질문]
> **면접관**: "답변 잘 들었습니다. 이어서, 대규모 트래픽 상황에서 DB 커넥션 풀(DBCP) 고갈 문제를 방지하기 위해 커넥션 풀 크기를 튜닝하거나 애플리케이션 레벨에서 고려해야 할 최적화 방안은 무엇인가요?"

* **최지아 답변** `[평가 점수: 4.7/5]` *(최우수 답변 - WORST 문항)*
  "커넥션 풀 최적화를 위해 무조건 풀 크기를 늘리기보다는, 톰캣 스레드 수와 DB CPU 코어 수의 관계를 고려해야 합니다. HikariCP 공식 가이드의 공식을 참고하여 스레드 경쟁 비용을 줄이는 크기로 설정했습니다. 그리고 애플리케이션 레벨에서는 트랜잭션 범위를 좁히는 것이 핵심입니다. 서비스 단 전체에 걸쳐 `@Transactional`을 붙이기보다 데이터 쓰기나 갱신이 일어나는 특정 지점으로 범위를 좁혔고, 단순 조회 쿼리는 `readOnly = true`를 활성화해 변경 감지 오버헤드를 막고 커넥션을 빨리 반환하도록 유도했습니다. 또한 OSIV 설정을 비활성화함으로써 뷰 렌더링 영역까지 커넥션이 유지되는 시간을 방지해 풀 고갈을 예방했습니다."

---

#### 👤 멘티 2: 정윤진 (AI 파이프라인 분야)

#### ❓ [개인 질문 1 - 추천 개인 질문 1번 기반]
> **면접관**: "다음으로 정윤진 지원자님께 질문 드리겠습니다. 대용량 WAV 파일 분석을 동기식 API 호출에서 Celery와 RabbitMQ를 활용한 비동기 작업 처리로 전환하셨는데, 태스크 유실 방지를 위해 RabbitMQ의 메시지 영속화(Message Durability)와 Celery의 수동 Acknowledgment 설정을 어떻게 하셨나요?"

* **정윤진 답변** `[평가 점수: 3.8/5]` *(우수 답변 - BEST 문항)*
  "RabbitMQ에서 메시지가 유실되지 않도록 큐를 생성할 때 durable 옵션을 true로 설정하고, 퍼블리셔가 전송할 때도 delivery_mode를 2로 지정해 디스크에 기록하도록 처리했습니다. 또한 Celery 설정에서는 기본 자동 ack 정책 대신, 비즈니스 로직 연산이 완전하게 끝나 정상 반환되었을 때만 RabbitMQ로 ACK 신호를 전송하도록 task_acks_late를 true로 활성화했습니다. 다만 이 설정을 켜면 처리 중 워커 장애 발생 시 메시지가 다시 큐로 돌아가 중복 실행될 위험이 있으므로, 데이터베이스 저장 단에 유니크 키 제약을 두어 멱등성을 지키도록 보완했습니다."

#### ❓ [개인 질문 2 - 추천 리스트에 없는 추가 개인 질문]
> **면접관**: "네. 그렇다면 AI 데이터 전처리 과정에서 대용량 파일 업로드 시 발생할 수 있는 메모리 부족(OOM) 문제를 예방하고 메모리 효율적으로 데이터를 파싱하는 전략은 무엇인가요?"

* **정윤진 답변** `[평가 점수: 3.5/5]` *(적당한 답변 - WORST 문항)*
  "대용량 오디오나 비디오 파일을 통째로 메모리에 로드하는 것은 매우 위험하므로, FastAPI에서 클라이언트의 파일 업로드 수신 시 UploadFile을 사용해 백그라운드에서 임시 디렉토리에 spooled 파일로 나누어 스트리밍 쓰기를 수행하도록 구현했습니다. 전처리 시 Pandas 라이브러리를 사용해 데이터를 파싱할 때도 대형 파일은 memory_limit에 걸리기 쉽기 때문에, read_csv나 parser 등에서 chunksize 옵션을 명시해 5,000행씩 데이터를 루프로 잘라서 읽어 들였고, 이를 bulk insert 쿼리로 DB에 분할 삽입하여 단시간 내 메모리 점유율이 튀는 현상을 안정적으로 방어했습니다."

---

#### 👤 멘티 3: 강동현 (실시간 통신 분야)

#### ❓ [개인 질문 1 - 추천 개인 질문 1번 기반]
> **면접관**: "마지막으로 강동현 지원자님께 질문 드리겠습니다. 다중 NestJS 서버 인스턴스 환경에서 소켓 세션을 공유하기 위해 Redis Pub/Sub을 사용하셨는데, 특정 인스턴스 장애로 연결이 끊어졌을 때 클라이언트의 재접속(Reconnection) 시나리오와 세션 복구 흐름은 어떻게 설계되어 있나요?"

* **강동현 답변** `[평가 점수: 2.5/5]` *(약간 부족한 답변 - BEST 문항)*
  "서버가 다운되면 클라이언트에 띄워진 Socket.io 클라이언트 모듈이 자동으로 감지해서 재접속을 하도록 기본 동작을 활용했습니다. 이때 로드밸런서인 Nginx가 살아있는 다른 NestJS 인스턴스로 소켓을 균등하게 재연결해 주기 때문에, 각 인스턴스들이 Redis Pub/Sub 어댑터로 메시지를 공유하고 있어 멘티들은 연결이 끊겼던 방에 바로 복귀해 세션을 이어나갈 수 있게 구현했습니다."

#### ❓ [개인 질문 2 - 추천 리스트에 없는 추가 개인 질문]
> **면접관**: "네, 알겠습니다. 실시간 채팅 중 네트워크 연결이 빈번하게 끊겼다 복구되는 불안정한 클라이언트 환경에서, 메시지 누락 없이 대화를 복구하는 재연결 정책과 메시지 시퀀스 넘버 관리 방안은 무엇인가요?"

* **강동현 답변** `[평가 점수: 2.2/5]` *(부족한 답변 - WORST 문항)*
  "네트워크 끊김으로 메시지 누락이 생기면 곤란하므로, 저는 소켓이 다시 연결되는 이벤트가 들어올 때 해당 룸의 모든 이전 대화 데이터를 데이터베이스 PostgreSQL에서 다시 SELECT 쿼리로 전체를 조회해서 클라이언트로 쫙 뿌려주는 방식을 사용했습니다. 시퀀스 넘버의 경우에는 아직 완벽히 구현하진 못했고, 그냥 데이터베이스 기본 키인 Auto Increment ID를 오름차순으로 정렬해서 클라이언트가 마지막으로 본 데이터가 무엇인지 순서만 비교해 렌더링하도록 짰습니다."
